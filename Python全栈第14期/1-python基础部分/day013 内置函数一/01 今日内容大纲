一. 昨日内容回顾
    生成器:
        生成器的本质是迭代器
    创建生成器的方式:
        1. 生成器函数
        2. 生成器表达式
        3. 类型转换

    生成器函数:  函数中包含yield. 生成器函数被执行的时候. 得到一个生成器.
        def func():
            yield
        gen = func()
        gen.__next__()  从开始执行到下一个yield
        send(内容) 给上一个yield传值

    推导式:
        1. 列表推导式 [结果 for循环 if判断]
        2. 字典推导式 {key:value  for循环 if}
        3. 集合推导式 {key for if}

    生成器表达式
        (结果 For循环 if判断)

    特性:
        1. 节省内存
        2. 惰性机制
        3. 只能向前.
    生成器其实存储的是代码.
二. 作业讲解

三. 今日主要内容:
    内置函数

    预习: lambda, sorted, filter, map, 递归(贪吃蛇), 二分法, 希尔排序(刘伟)
